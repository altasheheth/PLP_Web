
// included here is the code to make the tabs work
// When home is clicked the rest are hidden 
// const tabs = document.querySelectorAll('[data-tab-target]')
// to hide views as diferent tab is selected
// const tabContents = document.querySelectorAll("[data.-tab-content]")

// A loop through all the tabs
// tabs.forEach(tab => {
  // everytime the tab is clicked, the function below is run
//  tab.addEventListener("click", () => {
//    const target = document.querySelector(tab.dataset.tabTarget)
//    tabContents.forEach(tabContent => {
//      tabContent.classList.remove("active")
//    })
//    target.classlist.add('active')
//  })
//})